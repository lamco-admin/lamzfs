# Copyright 2026 Lamco Development LLC
#
# Licensed under the Apache License, Version 2.0 <LICENSE-APACHE> or the MIT
# license <LICENSE-MIT>, at your option. Forked/vendored upstream portions
# retain their own terms; see NOTICE.

[package]
name = "lamzfs"
version = "0.0.0"
edition = "2021"
authors = [
    "Greg Lamberson <greg@lamco.io>",
    "Lamco Development LLC <office@lamco.io>",
]
license = "MIT"
description = "no_std read-only ZFS reader for UEFI bootloaders (single/mirror/raidz1, unencrypted bpool)"
repository = "https://github.com/lamco-admin/lamzfs"
homepage = "https://lamco.ai/products/lamboot/"
readme = "README.md"
keywords = ["zfs", "filesystem", "no_std", "uefi", "bootloader"]
categories = ["filesystem", "embedded", "no-std"]
rust-version = "1.85"
documentation = "https://docs.rs/lamzfs"
include = [
    "src/**/*.rs",
    "README.md",
    "NOTICE",
    "CHANGELOG.md",
    "LICENSE-APACHE",
    "LICENSE-MIT",
]

[dependencies]
# Dependencies are added with the implementation (all permissive, upstream-cleared).

[features]
default = []
std = []

# Vendored rzfs SIMD-accelerated checksum features. Declared so `cfg(feature =
# ...)` in the vendored subtree resolves cleanly, but NEVER enabled: lamzfs
# compiles scalar Fletcher-2/4 + SHA-256 for portability (SPEC-LAMZFS §5.1).
fletcher2-sse2 = []
fletcher2-ssse3 = []
fletcher2-avx2 = []
fletcher2-avx512f = []
fletcher2-avx512bw = []
fletcher4-sse2 = []
fletcher4-ssse3 = []
fletcher4-avx2 = []
fletcher4-avx512f = []
fletcher4-avx512bw = []
sha256-bmi = []
sha256-ssse3 = []
sha256-avx = []
sha256-avx2 = []
sha256-sha = []

[package.metadata.docs.rs]
all-features = true
targets = ["x86_64-unknown-linux-gnu"]
rustdoc-args = ["--cfg", "docsrs"]

[badges]
maintenance = { status = "actively-developed" }

[lints.rust]
unsafe_op_in_unsafe_fn = "deny"
elided_lifetimes_in_paths = "warn"
unreachable_pub = "warn"

[lints.clippy]
pedantic = { priority = -1, level = "deny" }
missing_errors_doc = "allow"
module_name_repetitions = "allow"
must_use_candidate = "allow"
wildcard_imports = "deny"
unwrap_used = "warn"
expect_used = "warn"
